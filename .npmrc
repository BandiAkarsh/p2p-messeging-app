# pnpm Configuration
shamefully-hoist=true                                                   # Hoist dependencies like npm
strict-peer-dependencies=false                                          # Don't fail on peer dep issues
auto-install-peers=true                                                 # Auto install peer deps
node-linker=hoisted                                                     # Use hoisted node_modules
