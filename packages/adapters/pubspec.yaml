name: ecomesh_adapters
version: 1.0.0
description: Swappable adapter implementations for EcoMesh
publish_to: "none"

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  ecomesh_core:
    path: ../core
  http: ^1.2.0
  web_socket_channel: ^2.4.5
  cryptography: ^2.7.0
  libsignal_protocol_dart: ^0.7.0
  dart_jsonwebtoken: ^2.16.0
  shared_preferences: ^2.2.2

dev_dependencies:
  build_runner: ^2.4.9
  lints: ^5.1.1
  test: ^1.25.0
